# filen till utility funktioner som t.ex: 
# load_data(...) – kan vara csv/open eller pandas.read_csv

# från omniway:
# Minst 2 egna funktioner för beräkningar (t.ex. compute_revenue(...), top_categories(...)).